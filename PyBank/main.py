#Count num of rows-first row (to determine num of months)

#Read numbers in right column
#Cast numbers as integers
#Add all the numbers together

#set variables to 0
#Loop through all rows and get changes and add together 
#(TotalChange = TotalChange + CurrentChange)
#Divide the TotalChange by # of months (or months - 1)

#in the same loop
#if CurrentChange is > GreatInc, GreatInc = CurrentChange
#if CurrentChange < GreatDec, GreatDec = CurrentChange

#print analysis to terminal
#export to a file